[gd_resource type="ShaderMaterial" load_steps=8 format=3 uid="uid://cmep4fe0ihaeg"]

[ext_resource type="Texture2D" uid="uid://cvqe30nfh1vw3" path="res://assets/grass-texture_n.jpg" id="1_o5yji"]
[ext_resource type="Texture2D" uid="uid://83f87qyvlw8a" path="res://assets/grass-texture.jpg" id="1_thm45"]

[sub_resource type="Shader" id="Shader_nt11h"]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_disabled, diffuse_toon, specular_toon;

uniform vec4 albedo : source_color;
uniform sampler2D normal_map;
uniform sampler2D albedo_texture;
uniform sampler2D color_gradient;
uniform sampler2D fresnel_gradient;

//float fresnel(float amount, vec3 normal, vec3 view) {
	//return pow((1.0 - clamp(dot(normalize(normal), normalize(view)), 0.0, 1.0)), (amount / 0.25));
//}

void vertex() {
	// Called for every vertex the material is visible on.
}

void fragment() {
    vec3 normal_from_map = texture(normal_map, UV).rgb;
    normal_from_map = normal_from_map * 2.0 - 1.0; // Transform from [0,1] to [-1,1]
    NORMAL = mix(NORMAL, normal_from_map, 0.5); // Mix with the existing normal for effect
	
	vec4 tex_color = texture(albedo_texture, UV);
    ALBEDO = tex_color.rgb; // Use the RGB values from the texture for the albedo color
    //ALPHA = tex_color.a;   // Use the alpha channel of the texture, if necessary
	
	//float basic_fresnel = fresnel(4.0, NORMAL, VIEW);
	//ALBEDO = albedo.rgb;
}

void light() {
	// Called for every pixel for every light affecting the material.
	float dotproduct = clamp(dot(NORMAL, LIGHT), -0.9, 0.9);
	float sample = clamp((dotproduct + 1.0) * ATTENUATION / 2.1, 0.05, 0.95);
	vec4 shaded = texture(color_gradient, vec2(sample, 0.0));
	DIFFUSE_LIGHT += clamp((shaded.rgb * LIGHT_COLOR), 0.0, 1.0);
}
"

[sub_resource type="Gradient" id="Gradient_gj27f"]
offsets = PackedFloat32Array(0.204918, 0.877049, 1)
colors = PackedColorArray(0.051971, 0.051971, 0.051971, 1, 0.332881, 0.332881, 0.332881, 1, 1, 1, 1, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_dohqs"]
gradient = SubResource("Gradient_gj27f")

[sub_resource type="Gradient" id="Gradient_k3t5g"]
offsets = PackedFloat32Array(0.508197)
colors = PackedColorArray(0, 0, 0, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_hjrxg"]
gradient = SubResource("Gradient_k3t5g")

[resource]
render_priority = 0
shader = SubResource("Shader_nt11h")
shader_parameter/albedo = Color(1, 1, 1, 1)
shader_parameter/normal_map = ExtResource("1_o5yji")
shader_parameter/albedo_texture = ExtResource("1_thm45")
shader_parameter/color_gradient = SubResource("GradientTexture1D_dohqs")
shader_parameter/fresnel_gradient = SubResource("GradientTexture1D_hjrxg")
