[gd_resource type="ShaderMaterial" load_steps=6 format=3 uid="uid://j13pmukjir6o"]

[sub_resource type="Shader" id="Shader_wvkgu"]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_disabled, diffuse_toon, specular_toon;

uniform vec4 albedo : source_color;
uniform sampler2D color_gradient;
uniform sampler2D fresnel_gradient;

float fresnel(float amount, vec3 normal, vec3 view) {
	return pow((1.0 - clamp(dot(normalize(normal), normalize(view)), 0.0, 1.0)), amount);
}

void vertex() {
	// Called for every vertex the material is visible on.
}

void fragment() {
	float basic_fresnel = fresnel(4.0, NORMAL, VIEW);
	// Get a lighter version of the albedo color
    vec3 lightAlbedoColor = albedo.rgb * 3.0; // Adjust the multiplier to make it lighter or darker
    // Combine the albedo color with the fresnel effect
    ALBEDO = albedo.rgb + (basic_fresnel * lightAlbedoColor * texture(fresnel_gradient, vec2(basic_fresnel, 0.0)).rgb);
}

void light() {
	// Called for every pixel for every light affecting the material.
	float dotproduct = clamp(dot(NORMAL, LIGHT), -0.9, 0.9);
	float sample = clamp((dotproduct + 1.0) * ATTENUATION / 2.1, 0.05, 0.95);
	vec4 shaded = texture(color_gradient, vec2(sample, 0.0));
	DIFFUSE_LIGHT += clamp((shaded.rgb * LIGHT_COLOR), 0.0, 1.0);
}
"

[sub_resource type="Gradient" id="Gradient_0c4ub"]
offsets = PackedFloat32Array(0.581967, 1)
colors = PackedColorArray(0.0646965, 0.0646965, 0.0646965, 1, 1, 1, 1, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_35aor"]
gradient = SubResource("Gradient_0c4ub")

[sub_resource type="Gradient" id="Gradient_f5oos"]
offsets = PackedFloat32Array(0.901639, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_mub8j"]
gradient = SubResource("Gradient_f5oos")

[resource]
render_priority = 0
shader = SubResource("Shader_wvkgu")
shader_parameter/albedo = Color(0.235294, 0.235294, 0.258824, 1)
shader_parameter/color_gradient = SubResource("GradientTexture1D_35aor")
shader_parameter/fresnel_gradient = SubResource("GradientTexture1D_mub8j")
